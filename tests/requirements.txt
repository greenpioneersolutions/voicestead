# Single dependency source for every CI job: pip install -r tests/requirements.txt
# Pinned so weekly scorecard deltas measure the skill, not SDK drift.
anthropic>=0.57,<1      # known-good range; mock mode (VOICESTEAD_MOCK=1) never imports it
pytest==8.4.2
pyyaml==6.0.3
